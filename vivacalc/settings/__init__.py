"""Settings package.

Select an environment explicitly via DJANGO_SETTINGS_MODULE:

    vivacalc.settings.dev    local development
    vivacalc.settings.prod   production

There is intentionally no importable default here, so nothing can start with
an ambiguous configuration.
"""
